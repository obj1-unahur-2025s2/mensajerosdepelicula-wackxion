import mensajeros.*
import destinos.*
import paquetes.*
import herramientas.*
import vehiculos.*

 
describe "test pedidos por ejercisio" {

  test "Que roberto (90kg) puede llevar un paquete que está pago en un camión que tiene un acoplado." {
    roberto.cambioPeso(90)
    roberto.cambioVehiculo(camion)
    camion.agregar_Acoplados(1)
    assert.equals(false, roberto.puedeEntragar_En_(paquete, puenteBrooklyn))
  }

}